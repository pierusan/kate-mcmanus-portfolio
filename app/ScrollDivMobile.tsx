// Add a empty long scroll div on mobile which will control the characters
// flipped and the background color
export function ScrollDivMobile() {
  return <div className={'hidden coarse:block ' + 'h-[calc(1000dvh)] '} />;
}
